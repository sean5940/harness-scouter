
  M0.5 reproducibility gate — 4 closed periods

  not computable         Read-scope discipline
        o Saturation                 p05~p95 0.276                                > 0.069 (the value that killed cache_read)
        o Denominator density        median 64                                    >= budget 10
        o Variance components (prescription) signal/noise 3.42 · denominator for reliability 0.5 7 (budget 10) No threshold. This turns a failure into a prescription
        ? split-half                 not computable                               >= 0.5
        o Cross-period predictivity (display) lag-1 r=-0.385 · period-to-period delta is not supported No threshold. The view does not use delta
        o Personal-best reachability best 75.7 · denominator of that period 107   best period denominator >= 10 and inside the quartile fence
        ? Length confound            0.949 · p=not computable · periods 4         fail when |rho| >= 0.5 and p < 0.05; passing needs 12+ periods
        o Gaming shift (display)     +31.6p · 67% of headroom · 1.6x drift (Walk the same file in 3 chunks to raise only the partial-read ratio) No threshold. The two checks below decide
        o Ceiling unreachable        only up to 84.2                              Must not reach 100 through an activity-invariant path
        o Denominator survival       den 191 · scored periods 4/4                 den >= 10 and period count preserved

  not met                Check freshness
        o Saturation                 p05~p95 0.070                                > 0.069 (the value that killed cache_read)
        o Denominator density        median 15                                    >= budget 10
        o Variance components (prescription) true variance is below noise; a bigger denominator will not help No threshold. This turns a failure into a prescription
        ? split-half                 not computable                               >= 0.5
        o Cross-period predictivity (display) lag-1 r=-0.500 · period-to-period delta is not supported No threshold. The view does not use delta
        o Personal-best reachability best 7.1 · denominator of that period 14     best period denominator >= 10 and inside the quartile fence
        ? Length confound            -0.889 · p=not computable · periods 4        fail when |rho| >= 0.5 and p < 0.05; passing needs 12+ periods
        o Gaming shift (display)     +96.9p · 100% of headroom · 15.5x drift (Cover just one of the edited paths and have it count for all of them) No threshold. The two checks below decide
        X Ceiling unreachable        pushed to the ceiling (3.1 → 100.0)          Must not reach 100 through an activity-invariant path
        o Denominator survival       den 15 · scored periods 4/4                  den >= 10 and period count preserved

  not computable         Instrumented-channel use
        o Saturation                 p05~p95 0.308                                > 0.069 (the value that killed cache_read)
        o Denominator density        median 476                                   >= budget 20
        o Variance components (prescription) signal/noise 16.02 · denominator for reliability 0.5 6 (budget 20) No threshold. This turns a failure into a prescription
        ? split-half                 not computable                               >= 0.5
        o Cross-period predictivity (display) lag-1 r=-0.722 · period-to-period delta is not supported No threshold. The view does not use delta
        o Personal-best reachability best 71.1 · denominator of that period 311   best period denominator >= 20 and inside the quartile fence
        ? Length confound            -0.316 · p=not computable · periods 4        fail when |rho| >= 0.5 and p < 0.05; passing needs 12+ periods
        o Gaming shift (display)     +24.0p · 64% of headroom · 0.8x drift (Chain bash file accesses 4:1 with && to cut only the call count) No threshold. The two checks below decide
        o Ceiling unreachable        only up to 86.7                              Must not reach 100 through an activity-invariant path
        o Denominator survival       den 296 · scored periods 4/4                 den >= 20 and period count preserved

  not met                Read round-trip restraint
        o Saturation                 p05~p95 0.091                                > 0.069 (the value that killed cache_read)
        o Denominator density        median 225                                   >= budget 20
        o Variance components (prescription) signal/noise 0.56 · denominator for reliability 0.5 71 (budget 20) No threshold. This turns a failure into a prescription
        ? split-half                 not computable                               >= 0.5
        o Cross-period predictivity (display) lag-1 r=0.742 · period-to-period delta is not supported No threshold. The view does not use delta
        o Personal-best reachability best 93.5 · denominator of that period 124   best period denominator >= 20 and inside the quartile fence
        ? Length confound            0.211 · p=not computable · periods 4         fail when |rho| >= 0.5 and p < 0.05; passing needs 12+ periods
        o Gaming shift (display)     +7.3p · 100% of headroom · 6.9x drift (A trivial edit right before a re-read relabels the revisit as a legitimate recheck) No threshold. The two checks below decide
        X Ceiling unreachable        pushed to the ceiling (92.7 → 100.0)         Must not reach 100 through an activity-invariant path
        o Denominator survival       den 225 · scored periods 4/4                 den >= 20 and period count preserved

  not met                Check redundancy restraint
        o Saturation                 p05~p95 0.183                                > 0.069 (the value that killed cache_read)
        o Denominator density        median 33                                    >= budget 10
        o Variance components (prescription) signal/noise 2.77 · denominator for reliability 0.5 6 (budget 10) No threshold. This turns a failure into a prescription
        ? split-half                 not computable                               >= 0.5
        o Cross-period predictivity (display) lag-1 r=-0.362 · period-to-period delta is not supported No threshold. The view does not use delta
        o Personal-best reachability best 90.9 · denominator of that period 22    best period denominator >= 10 and inside the quartile fence
        ? Length confound            -0.211 · p=not computable · periods 4        fail when |rho| >= 0.5 and p < 0.05; passing needs 12+ periods
        o Gaming shift (display)     +10.9p · 100% of headroom · 0.6x drift (Slip a trivial edit or file write between checks to reset the same-kind decision) No threshold. The two checks below decide
        X Ceiling unreachable        pushed to the ceiling (89.1 → 100.0)         Must not reach 100 through an activity-invariant path
        o Denominator survival       den 33 · scored periods 4/4                  den >= 10 and period count preserved

  not computable         Index-first retrieval
        o Saturation                 p05~p95 0.348                                > 0.069 (the value that killed cache_read)
        o Denominator density        median 65                                    >= budget 10
        o Variance components (prescription) signal/noise 4.32 · denominator for reliability 0.5 5 (budget 10) No threshold. This turns a failure into a prescription
        ? split-half                 not computable                               >= 0.5
        o Cross-period predictivity (display) lag-1 r=-0.505 · period-to-period delta is not supported No threshold. The view does not use delta
        o Personal-best reachability best 43.6 · denominator of that period 39    best period denominator >= 10 and inside the quartile fence
        ? Length confound            0.738 · p=not computable · periods 4         fail when |rho| >= 0.5 and p < 0.05; passing needs 12+ periods
        o Gaming shift (display)     +36.8p · 45% of headroom · 1.6x drift (Run one index query whose results are never read before each scan) No threshold. The two checks below decide
        o Ceiling unreachable        only up to 55.3                              Must not reach 100 through an activity-invariant path
        o Denominator survival       den 118 · scored periods 4/4                 den >= 10 and period count preserved

  4 axis-independence violations:
        Read-scope discipline ~ Check freshness  rho=-0.949
        Read-scope discipline ~ Index-first retrieval  rho=0.800
        Check freshness ~ Index-first retrieval  rho=-0.949
        Read round-trip restraint ~ Check redundancy restraint  rho=-1.000

  All-time view   0/6 axes supported · 3 failed · 3 not computable
  Per-period view 0/6 axes supported · 3 failed · 3 not computable
